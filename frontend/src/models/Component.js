export class Component {

    constructor(id, name, typeId, selected, selectOptions) {
        this.id = id;
        this.name = name;
        this.typeId = typeId;
        this.selected = selected;
        this.selectOptions = selectOptions;
    }
}